import {observer} from 'mobx-react-lite';
import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const InputLocationComp = observer(() => {
  return (
    <View>
      <Text></Text>
    </View>
  );
});

export default InputLocationComp;

const styles = StyleSheet.create({});
